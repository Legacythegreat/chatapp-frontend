export const config = {
  backendUrl: "https://chatapp-backend-sigma-two.vercel.app",
  // Alternatively, you can use environment variables:
  // backendUrl: process.env.REACT_APP_BACKEND_URL || "http://localhost:4000"
};